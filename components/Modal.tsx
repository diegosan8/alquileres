import React from 'react';

const Modal = ({ children, title, onClose, size = 'xl', zIndex = 50 }) => {
    const sizeClasses = {
        sm: 'max-w-sm',
        md: 'max-w-2xl',
        xl: 'max-w-4xl',
        '4xl': 'max-w-7xl'
    };
    return (
        // ... Copia aquí el JSX del modal ...
        <div>Modal</div>
    );
};

export default Modal;
